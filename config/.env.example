# ================================
# IMAP邮件提取和CSV导出系统 - 环境变量配置
# ================================
# 
# 使用说明:
# 1. 复制本文件为 .env: cp config/.env.example .env
# 2. 填写实际的配置值(特别是邮箱账号和密码)
# 3. .env 文件包含敏感信息,不应提交到版本控制系统
#

# ================================
# 邮箱认证信息 (必需)
# ================================

# 邮箱账号
# 示例: your-email@gmail.com
EMAIL_ACCOUNT=

# 邮箱密码或应用专用密码
# 注意: 
# - Gmail用户请使用应用专用密码,不要使用账号密码
# - 应用专用密码生成方法: Google账号 > 安全性 > 两步验证 > 应用专用密码
EMAIL_PASSWORD=


# ================================
# IMAP服务器配置
# ================================

# IMAP服务器地址
# 常见邮箱服务器:
# - Gmail: imap.gmail.com
# - Outlook: outlook.office365.com
# - QQ邮箱: imap.qq.com
# - 163邮箱: imap.163.com
IMAP_HOST=imap.gmail.com

# IMAP端口 (默认: 993)
IMAP_PORT=993

# 是否使用SSL连接 (默认: true)
# 可选值: true, false
IMAP_USE_SSL=true

# 连接超时时间(秒) (默认: 30)
IMAP_TIMEOUT=30


# ================================
# 邮件配置
# ================================

# 默认邮箱文件夹 (默认: INBOX)
# 常见文件夹: INBOX, Sent, Drafts, Trash
EMAIL_FOLDER=INBOX

# 读取后是否标记为已读 (默认: false)
# 可选值: true, false
EMAIL_MARK_AS_READ=false

# 批量处理邮件数量 (默认: 50)
# 建议值: 10-100
EMAIL_BATCH_SIZE=50


# ================================
# 筛选配置
# ================================

# 是否启用筛选功能 (默认: true)
# 可选值: true, false
FILTER_ENABLED=true

# 默认最大邮件数量限制 (默认: 1000)
# 设置为 0 表示不限制
FILTER_DEFAULT_LIMIT=1000


# ================================
# 输出配置
# ================================

# CSV文件输出目录 (默认: output/csv)
# 支持相对路径和绝对路径
OUTPUT_CSV_DIR=output/csv

# CSV文件名 (默认: 空,使用时间戳命名)
# 留空则自动生成如: emails_20240115_143025.csv
OUTPUT_CSV_FILENAME=

# 附件保存目录 (默认: output/attachments)
OUTPUT_ATTACHMENT_DIR=output/attachments

# 是否保存附件 (默认: true)
# 可选值: true, false
OUTPUT_SAVE_ATTACHMENTS=true


# ================================
# 日志配置
# ================================

# 日志级别 (默认: INFO)
# 可选值: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# 日志格式
# 默认格式会显示: [时间] [级别] [文件:行号] 消息
LOG_FORMAT=[%(asctime)s] [%(levelname)s] [%(filename)s:%(lineno)d] %(message)s

# 日志文件路径 (默认: logs/app.log)
LOG_FILE=logs/app.log


# ================================
# 高级配置 (可选)
# ================================

# 以下配置项可以通过命令行参数覆盖:
# --email            : 邮箱账号
# --password         : 邮箱密码
# --host             : IMAP服务器地址
# --port             : IMAP端口
# --folder           : 邮箱文件夹
# --from-date        : 起始日期 (YYYY-MM-DD)
# --to-date          : 结束日期 (YYYY-MM-DD)
# --sender           : 发件人过滤
# --subject          : 主题关键词
# --labels           : 标签过滤 (逗号分隔)
# --limit            : 最大邮件数量
# --output           : CSV输出路径
# --filename         : CSV文件名
# --save-attachments : 是否保存附件
# --attachment-dir   : 附件目录
# --log-level        : 日志级别

# 配置优先级: 命令行参数 > 环境变量 > 默认值